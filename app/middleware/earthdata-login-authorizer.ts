/**
 * STUBBED Express.js middleware for doing EDL auth
 *
 * @param {http.IncomingMessage} req The client request
 * @param {http.ServerResponse} res The client response
 * @param {function} next The next function in the middleware chain
 * @returns {void}
 */
module.exports = function earthdataLoginAuthorizer(req, res, next) {
  next();
};
