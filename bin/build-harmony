#!/bin/bash

# build the cmr granule locator task image
# cd tasks/query-cmr
# docker build --tag query-cmr:latest .
# cd ../..

# build the harmony-local docker image
docker build --tag harmony/harmony-local:latest .

# build the cmr-granule-locator image
docker build --tag harmony/query-cmr:latest -f ./Dockerfile.cmr-granule-locator-k8s .

